import React from 'react';

const Contato: React.FC = () => {
    return (
        <div>
            <h1>Contato</h1>
            {/* Conteúdo da página de Contato */}
        </div>
    );
};

export default Contato;