import React from 'react';

const Produtos: React.FC = () => {
    return (
        <div>
            <h1>Produtos</h1>
            {/* Conteúdo da página de produtos */}
        </div>
    );
};

export default Produtos;